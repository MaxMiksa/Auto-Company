---
title: This frontmatter fence never closes

# Real Heading

Body text that should still be discoverable as a fallback title source
since the "frontmatter" above never terminates and must be treated as
ordinary body content instead.
