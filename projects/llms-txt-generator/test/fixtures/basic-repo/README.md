# Basic Repo

This is a sample project used to test zero-config llms.txt generation.
It has no frontmatter, so the generator should fall back to this H1 for
the title and to this paragraph for the description.
