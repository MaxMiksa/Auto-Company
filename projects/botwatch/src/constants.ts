/** Route prefix for the dashboard (HTML shell + JSON API). Shared between
 *  worker.ts (routing) and dashboard.ts (sub-path parsing) so they can't drift. */
export const DASHBOARD_PREFIX = "/botwatch-dashboard";
